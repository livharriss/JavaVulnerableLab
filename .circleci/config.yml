version: 2.1

orbs:
  maven: circleci/maven@0.0.12

workflows:
  maven_test:
    jobs:
      - maven/test: 
          command: 'mvn compile com.softwaresecured.reshift:maven-plugin:LATEST:analyse \
                  com.softwaresecured.reshift:maven-plugin:LATEST:upload \
                  -Dreshift.token=$RESHIFT_TOKEN'
